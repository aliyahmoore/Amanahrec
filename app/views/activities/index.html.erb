<% content_for :title, "Upcoming Activities" %>
<h1 class="mt-3 text-center">Upcoming Activities</h1>
<div class="row d-flex justify-content-center g-3">
  <% @activities.each do |activity| %>
    <%= render 'shared/item_card', item: activity %>
  <% end %>
</div>