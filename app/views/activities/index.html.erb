<% content_for :title, "Upcoming Activities & Events" %>
<h1 class="mt-3 text-center">Upcoming Activities & Events</h1>
<div class="row d-flex justify-content-center g-3 mt-5">
  <% @activities.each do |activity| %>
    <%= render 'shared/item_card', item: activity %>
  <% end %>
</div>
