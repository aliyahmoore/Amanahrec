<% content_for :title, "Upcoming Events" %>
<h1 class="mt-3 text-center">Upcoming Events</h1>
<div class="row d-flex justify-content-center g-3">
  <% @events.each do |event| %>
    <%= render 'shared/item_card', item: event %>
  <% end %>
</div>