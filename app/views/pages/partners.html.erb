<% content_for :title, "Our Partners" %>
<h1 class="text-center mt-3">Our Partners</h1>
<div class="row justify-content-center gap-3 mt-5">
  <% @partners.each do |partner| %>
    <div class="col-auto d-flex justify-content-center">
      <div class="partner-logo">
        <% if partner.logo.attached? %>
          <% if partner.link.present? %>
            <%= link_to partner.link, target: "_blank" do %>
              <%= image_tag partner.logo, alt: partner.name, class: "img-fluid", style: "height: 100px;" %>
            <% end %>
          <% else %>
            <%= image_tag partner.logo, alt: partner.name, class: "img-fluid", style: "height: 100px;" %>
          <% end %>
        <% else %>
          <p>No Logo Available</p>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
